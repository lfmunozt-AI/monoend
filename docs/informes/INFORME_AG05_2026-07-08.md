# INFORME AG05 — Documentación integral del sprint

Fecha: 2026-07-08
Agente: AG05 — Documentación
Worktree: `wt-ag05-docs` · Branch: `agent/05`
Base: `origin/develop` @ `c10c511`

## Misión

Poner la documentación al día tras varias tandas sin documentar: PROJECT_LOG,
reescritura mayor de CLAUDE.md (ADN + terminología + proceso + backlog),
`docs/GUARDRAIL.md`, y reubicación de los informes.

## Output entregado

| Archivo | Acción |
|---|---|
| `CLAUDE.md` | Reconstruido sobre `b4f24d5` + 8 sobreescrituras de hoy |
| `PROJECT_LOG.md` | +4 entradas fechadas (05-26, 07-06, 07-08) |
| `docs/GUARDRAIL.md` | Nuevo — 8 secciones |
| `docs/informes/INFORME_SPRINT_MIGRACION.md` | `git mv` desde la raíz (rename detectado, historia preservada) |
| `docs/informes/INFORME_AG05_2026-07-08.md` | Este archivo |

## Decisión de base para CLAUDE.md

`origin/develop` traía el CLAUDE.md **pre-pivot** del 2026-05-19 (271 líneas,
historial de bugfixes embebido, "Reserva de Soberanía"). La reescritura post-pivot
de `agent/05` (`b4f24d5`, 215 líneas, estructura blueprint) **nunca se mergeó a
develop** — quedó huérfana en la rama del agente.

Confirmado con Luis: **restaurar `b4f24d5` como base estructural** y aplicar encima
las decisiones de hoy, que siempre ganan. El historial de bugfixes de mayo pasa a
`PROJECT_LOG.md`, que es su sitio.

## Las 8 sobreescrituras aplicadas

| # | b4f24d5 (base) | 2026-07-08 (gana) |
|---|---|---|
| 1 | ICA = "Índice de Conocimiento del Asistido" | **ICA = "Índice de Certeza Algorítmica"** (provisional unificado) |
| 2 | IDF = "Índice de Dominio Financiero" | **IDF** sin expandir · métrica de progreso hacia la meta. *Dominio Financiero* es el nivel, no el índice |
| 3 | "Reserva de Soberanía" (terminología + regla 7) | **"Reserva de Imprevistos"** (multiidioma), 0 ocurrencias de la anterior |
| 4 | Regla 7: prohibido en UI, "sí como término técnico" | Regla 7: **prohibido en TODO, sin excepción** |
| 5 | — | Bloque **`---ADN---`** al inicio del archivo |
| 6 | "The Consigliere: agente IA" | **nombre INTERNO del modelo**; en UI solo badge, nunca en el cuerpo |
| 7 | — | **Tercera vía de cifras estándar** + **sprint 5 capas** (backlog DONE + decisiones técnicas) |
| 8 | 3 productos · worktrees · fórmulas · decisiones · backlog fundacional | **conservados** |

Añadido además, no cubierto por las 8: sección REGLAS DE PROCESO (alias
`git-develop`, PRs base `develop`, `--force-with-lease` solo sobre `agent/XX`,
informes en `docs/informes/`), regla de credenciales del LLM Router, y `monoend`
como nombre comercial del B2C.

## Discrepancias detectadas (acción para Luis)

1. **Las 5 capas están migradas, pero 4 de 5 no están en el camino de la request.**
   Verificado con `grep` sobre los importadores, no por lectura de commits:
   - `detectInjection()` — solo se reexporta en `guardrail/index.ts`. Nadie la llama.
   - `buildVerifiedContext()` — solo se usa en `orchestrator.test.ts`. El prompt no
     recibe el bloque de cifras verificadas, y `validateGrounding()` se invoca sin
     su tercer argumento `cifrasCalculadas`, que por eso siempre vale `[]`.
   - `src/lib/llm/router.ts` — ningún módulo lo importa. `api/chat/route.ts` y
     `api/transactions/route.ts` siguen en el viejo `src/lib/llm.ts`.

   Documenté el sprint como **DONE** (las capas están migradas, que es lo que se
   pidió) y abrí un pendiente explícito por cada cableado que falta. La tabla de
   `CLAUDE.md` → DECISIONES TÉCNICAS distingue *migrado* de *en la request*.

2. **`src/lib/guardrail/schema.ts` importa `zod`, que no está en `package.json`.**
   Hoy resuelve como dependencia transitiva de `openai` (`zod@4.4.3` en el lock).
   Si `openai` deja de arrastrarlo, el build rompe sin que nadie haya tocado el
   guardarraíl. **Declararlo explícitamente** (AG01 o AG08).

3. **`getICALevel()` devuelve el literal `'soberania'`** (`src/lib/ica.ts:112`) y
   `api/chat/route.ts:258` lo persiste en `ica_history.level`. Viola la regla
   absoluta 7 en datos, no solo en código. Migración + backfill → **AG06**.

4. **La docstring de `guardrail/index.ts` está desactualizada.** Dice
   *"ENGANCHE (no cableado en esta sesión)"*, pero `runGuardrail` **sí** está
   cableado en el chat route desde `77426ae`. Corregirla (AG08).

5. **`feature/streaming-buffer` existe solo en local**, no en `origin`. Si el
   worktree se pierde, el trabajo de streaming se pierde con él.

6. **`INFORME_SPRINT_MIGRACION.md` no cumple la regla que inaugura.** Es un
   compuesto de 3 informes / 2 agentes / 2 fechas. Lo moví íntegro (instrucción
   explícita) con una cabecera que lo marca como legado. Si prefieres, lo divido
   en `INFORME_AG02_2026-05-26.md`, `INFORME_AG08_2026-05-26.md` e
   `INFORME_AG08_2026-07-06.md`.

7. **Un `INFORME.md` sin trackear quedó en el worktree** (AG05, 2026-05-26, sobre
   la reescritura post-pivot). No estaba en `develop`. Respaldado antes del
   `reset --hard`; no lo he re-añadido al repo. Decir si debe entrar como
   `docs/informes/INFORME_AG05_2026-05-26.md`.

## Restricciones respetadas

- Solo archivos de mi dominio: `CLAUDE.md`, `PROJECT_LOG.md`, `docs/`. **Ningún
  archivo de `src/` ni de `supabase/` tocado.**
- Push solo a `agent/05`, con `--force-with-lease` (la rama remota divergía tras
  el `reset --hard origin/develop`; autorizado por Luis sobre la propia rama).
- Sin `push` a `main` ni `develop`.

## Validación

```
npm ci            → ok (node_modules no existía en el worktree)
npm run build     → ver sección de entrega
```

Los 5 archivos tocados son `.md`; ninguno entra en el grafo de módulos de Next.js.
El build se corrió igualmente, como pide el protocolo.

## Siguiente paso recomendado

1. Cablear el LLM Router (AG08) — es la capa con más coste de divergencia: cada
   día que pasa, más consumidores nuevos se escriben contra `src/lib/llm.ts`.
2. Declarar `zod` en `package.json` (AG01).
3. Migración + backfill de `'soberania'` en `ica_history.level` (AG06).
4. Renombrar el repo a `monoend` antes de que la URL de Vercel y los 10 remotes
   se fosilicen.
